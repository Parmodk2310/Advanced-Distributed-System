apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: distsys-phase7-k3d
servers: 1
agents: 0
options:
  k3s:
    extraArgs:
      - arg: --disable=traefik
        nodeFilters:
          - server:*
  kubeconfig:
    updateDefaultKubeconfig: true
    switchCurrentContext: true
