The {{ include "distributed-system.fullname" . }} release is internal-only.

Check readiness:
  kubectl -n {{ .Release.Namespace }} rollout status statefulset/{{ include "distributed-system.fullname" . }}

Use a private port-forward for verification:
  kubectl -n {{ .Release.Namespace }} port-forward service/{{ include "distributed-system.fullname" . }}-metrics 9100:{{ .Values.service.observabilityPort }}
