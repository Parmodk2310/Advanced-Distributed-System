The {{ include "distributed-system.fullname" . }} release is internal-only.
Wait for recovery-aware readiness:
  kubectl -n {{ .Release.Namespace }} rollout status statefulset/{{ include "distributed-system.fullname" . }}
